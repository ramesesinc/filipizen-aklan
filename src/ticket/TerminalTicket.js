import React, { useState } from "react";
import {
  Panel,
  Text,
  Button,
  FormPanel,
  ActionBar,
  Label,
  Spacer,
  Service,
  Error,
  Title,
  Table,
  TableColumn,
  BackLink,
  currencyFormat,
  Decimal
} from "rsi-react-web-components";

  // import PayOption from "../components/PayOption";

const txntype = "waterworks";
const origin = "filipizen";

const TerminalTicket = (props) => {
  return <h1>TerminalTicket</h1>
};

const styles = {

};

export default TerminalTicket;
